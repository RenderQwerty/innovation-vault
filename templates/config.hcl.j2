ui = true
max_lease_ttl = "10h"
default_lease_ttl = "10h"

listener "tcp" {
    address = "127.0.0.1:{{ backend_port }}",
    tls_disable = 1
}

api_addr = "http://127.0.0.1:{{ backend_port }}"

storage "file" {
    path = "{{ vault_data_path }}"
}
